{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "test": {
      "inputs": [
        "src/**",
        "config/**"
      ],
      "outputs": [
        "docs/**"
      ],
      "dependsOn": [
        "^test"
      ]
    },
    "fix": {
      "inputs": [
        "src/**"
      ]
    },
    "lint": {
      "inputs": [
        "src/**"
      ]
    },
    "build": {
      "inputs": [
        "src/**"
      ],
      "outputs": [
        "dist/**"
      ],
      "dependsOn": [
        "^build"
      ]
    },
    "check-type": {
      "inputs": [
        "src/**"
      ],
      "dependsOn": [
        "^check-type"
      ]
    }
  }
}
