{
  "name": "storevent",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "all": "npm run all:test && npm run all:fix && npm run all:lint && npm run all:type-check && npm run all:build",
    "all:test": "npm run test --ws",
    "all:fix": "npm run fix --ws",
    "all:lint": "npm run lint --ws",
    "all:type-check": "npm run type-check --workspace=packages/",
    "all:build": "BUILD_DIR=$PWD/builds npm run build --ws"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shinetools/banking-platform.git"
  },
  "author": "Banking Platform Squad",
  "bugs": {
    "url": "https://github.com/shinetools/banking-platform/issues"
  },
  "homepage": "https://github.com/shinetools/banking-platform#readme",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "engines": {
    "node": "20.18.0",
    "npm": "^10.8.2"
  }
}